name: Functional tests (anonymous)

on:
  pull_request:
  workflow_call:
  workflow_dispatch:

jobs:
  functional:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install uv
        uses: astral-sh/setup-uv@v6
      - name: Run functional tests
        run: uvx --from rust-just just test-functional
